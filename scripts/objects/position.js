// @ts-check

class Position {
    /**
     *
     * @param {Number} x
     * @param {Number} y
     */
    constructor(x, y) {
        this.x = parseInt(x, 10);
        this.y = parseInt(y, 10);
    }
}
